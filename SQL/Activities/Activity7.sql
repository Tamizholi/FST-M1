select sum(purchase_amount) as "TotalSum" from orders;

select sum(purchase_amount) as "TotalSum" from orders;

select avg(purchase_amount) as "AvgAmount" from orders;

select max(purchase_amount) as "Max Amount" from orders;

select min(purchase_amount) as "Min Amount" from orders;

select count(salesman_id) as "Total Salesman" from orders;

